'use client';
import {ArrowUpRight,Cloud,Shield,Users} from 'lucide-react';
import {Button} from '@/components/ui/button';
import type {OnlineStatus} from '@/game/simulation/online';
export function AccountGate({status,onCreate,onOffline}:{status:OnlineStatus;onCreate:()=>void;onOffline:()=>void}){if(status==='online')return null;return <section className="account-gate"><div className="account-card"><span className="account-mark">H<span>09</span></span><div className="account-copy"><div className="creator-kicker">HOODSCAPE ONLINE / SAINT MERCER</div><h1>{status==='loading'?'CONNECTING TO THE CITY':status==='signed_out'?'YOUR CITY NEEDS A NAME':status==='offline'?'THE CITY IS OFFLINE':'CLAIM YOUR ONLINE ID'}</h1><p>{status==='loading'?'Checking your account and cloud character…':status==='signed_out'?'Create your player account with ChatGPT. Your character, progress, reputation, and shared-world identity follow you to any device.':status==='offline'?'Shared players are unavailable right now. You can still enter your device-local story and reconnect later.':'One account. One persistent character. The name you choose becomes your identity in the shared world.'}</p>{status==='loading'?<div className="account-loading"><i/><span>SYNCING DISTRICT 09</span></div>:status==='signed_out'?<a className="account-primary" href="/signin-with-chatgpt?return_to=%2F" target="_top">CREATE ACCOUNT / SIGN IN <ArrowUpRight size={19}/></a>:status==='setup'?<Button className="account-primary" onClick={onCreate}>CREATE YOUR CHARACTER <ArrowUpRight size={19}/></Button>:<Button className="account-primary" onClick={onOffline}>PLAY OFFLINE <ArrowUpRight size={19}/></Button>}<div className="account-features"><span><Users/>LIVE PLAYERS<small>Shared Saint Mercer</small></span><span><Cloud/>CLOUD SAVE<small>One character everywhere</small></span><span><Shield/>SAFE + PVP<small>You choose the risk</small></span></div></div></div></section>}
