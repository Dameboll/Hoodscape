# HOODSCAPE uses platform-provided authentication and D1 bindings when hosted.
# Keep this file as documentation only. Never commit real credentials.

# Local development does not require secrets for the IndexedDB/offline slice.
# Hosted Sites supplies the DB binding declared in .openai/hosting.json.
